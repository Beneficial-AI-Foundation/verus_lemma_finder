{
  "phases": {
    "setup": {
      "nixPkgs": ["python312", "gcc"]
    },
    "install": {
      "cmds": ["pip install -e '.[demo]'"]
    }
  },
  "start": {
    "cmd": "uvicorn demo.server:app --host 0.0.0.0 --port $PORT"
  }
}

