# Render deployment config (not used - using Railway instead)
# Keeping for reference
services:
  - type: web
    name: verus-lemma-finder-demo
    env: python
    region: oregon
    plan: free
    branch: main
    startCommand: uvicorn demo.server:app --host 0.0.0.0 --port $PORT

